import { FC } from 'react';
import Layout from '../Layout';

const CVPreview: FC = () => {
  return (
    <Layout className="bg-dark-gray">
      <h1>Preview</h1>
    </Layout>
  );
};

export default CVPreview;
